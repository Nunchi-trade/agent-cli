"""yields.sources — yield-opportunity discovery.

`defillama` provides broad read-only discovery across ~all Ethereum + Base
pools. `onchain/` holds the curated adapters that additionally read live
APY/TVL and a wallet's positions, and build executable deposit/withdraw routes.
"""
